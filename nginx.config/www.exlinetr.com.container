server {

    listen 80 default_server;
    listen [::]:80 default_server;

    root /apps/www.exlinetr.com/var/html;
    #root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }

}